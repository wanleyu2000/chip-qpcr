interface:
  display_name: "ChIP-qPCR Site Designer"
  short_description: "Predict TF promoter sites and analyze ChIP-qPCR"
  default_prompt: "Use $chip-qpcr to find the top two JASPAR motif candidates for my protein and gene, then prepare ChIP-qPCR validation outputs."
